import styled from 'styled-components';

const SignUp = () => {
    return (
        <Wrapper className="flex flex-column justify-center items-center">SignUp</Wrapper>
    );
};

const Wrapper = styled.div`
    min-width: 100vw;
    min-height: 100vh;
`;

export default SignUp;
